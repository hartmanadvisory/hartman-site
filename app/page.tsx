import Hero from "@/components/Hero";

// "Citadel for Hartman" rebuild — Step 1: Nav + Hero. Remaining movements
// (Who We Are, Track Record, What We Do, Who We Serve, The Record, CTA) land
// section by section, each gated on approval.
export default function Home() {
  return (
    <>
      <Hero />
    </>
  );
}
